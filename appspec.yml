version: 0.0
os: linux
files:
  - source: /
    destination: /appdir
hooks:
  ApplicationStop:
    - location: appdir/stop.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: appdir/start.sh
      timeout: 300
      runas: root
