version: 0.0
os: linux
files:
  - source: /
    destination: /appdir
hooks:
  ApplicationStop:
    - location: /appdir/scripts/stop.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: /appdir/scripts/start.sh
      timeout: 300
      runas: root
